// Stub mínimo — reescrito pelo SPRINT-08 (Kanban de sistemas, RF-01).
import type { JSX } from "react";

export function AikSystemsBoard(): JSX.Element {
  return <div data-testid="aik-systems-board" />;
}

export default AikSystemsBoard;
