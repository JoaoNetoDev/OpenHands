// Stub mínimo — reescrito pelo SPRINT-10 (Kanban de tarefas de uma fase,
// RF-10).
import type { JSX } from "react";

export function AikTasksBoard(): JSX.Element {
  return <div data-testid="aik-tasks-board" />;
}

export default AikTasksBoard;
